* To build the project with Maven, use the following command from grpcOrderService directory.

```javascript
:\> mvn package
```

* To spin up the microservice, run the following command from sample05 directory.

```javascript
:\> mvn spring-boot:run
```

